# Package index

## All functions

- [`infer_boot()`](boot.md) [`boot()`](boot.md) :

  Inference for `selector` via bootstrapping

- [`ciratio()`](ciratio.md) : Computing mean/median CI ratio and
  significant Discoveries

- [`hers`](hers.md) : HERS data example

- [`as_inferrer()`](infer.md) [`infer()`](infer.md)
  [`infer_pipe()`](infer.md) [`infer_selective()`](infer.md)
  [`infer_upsi()`](infer.md) :

  Constructor for `inferrer`s

- [`tidy(`*`<inferrer>`*`)`](inferrer.md)
  [`print(`*`<inferrer>`*`)`](inferrer.md) :

  tidy method for `inferrer` object

- [`select_full_model()`](select_full_model.md) : Select a full model

- [`select_glmnet()`](select_glmnet.md) : Fit an (relaxed) lasso or
  elastic net penalized regression via glmnet

- [`select_ncvreg()`](select_ncvreg.md) :

  Fit an MCP- or SCAD-penalized regression via `ncvreg`

- [`select_null_model()`](select_null_model.md) : Select a null model

- [`select_stepwise_ic()`](select_stepwise_ic.md) : Stepwise
  forward/backward/bidirectional selection with AIC/BIC

- [`as_selector()`](selector.md)
  [`predict(`*`<selector>`*`)`](selector.md)
  [`tidy(`*`<selector>`*`)`](selector.md)
  [`print(`*`<selector>`*`)`](selector.md)
  [`coef(`*`<selector>`*`)`](selector.md) [`reselect()`](selector.md) :

  Constructor for `selector`s
